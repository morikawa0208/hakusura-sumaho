# hakusura-sumaho

## PR 作成手順
このリポジトリでは、コミット後に `make_pr` ツールを実行しないと PR が生成されません。以下の手順で実行してください。

1. 変更をステージングする: `git add <変更したファイル>`
2. コミットする: `git commit -m "変更内容"`
3. PR を生成する: `make_pr` ツールを実行し、タイトルと本文を入力する

コミットを行っても `make_pr` を呼び出さなければ PR は作成されないので、必ず 2 の後に 3 を実施してください。
